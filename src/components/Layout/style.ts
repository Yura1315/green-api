import { Box, styled } from '@mui/material';

export const PageWrapper = styled('div') ({
    minHeight: '100vh',
    backgroundColor: '#eae6df',
    backgroundImage: 'linear-gradient(180deg, #eae6df, #d1d7db)',
    boxSizing: 'border-box',
    padding: 0,
    margin: 0
})